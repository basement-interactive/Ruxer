# Open Questions — values that could not be sourced

Per the brief: any value I could not trace to a specific file:line in
`./reference/fluxer/` is listed here instead of being invented. Each entry
notes what I searched.

## Substitutions made (per operating notes)

1. **No `refactor` branch on the remote.** `git ls-remote --heads https://github.com/fluxerapp/fluxer.git` returns only `main`, `fix/dev-desktop-canary-plist`, `weblate/translations`. The brief said "the refactor branch, since prod runs from it" — it does not exist. **Substitution**: using `main` (the default + prod branch, confirmed via `git ls-remote` HEAD). Verified `fluxer_app/` exists on `main` before cloning.
2. **No `media/app-showcase.png`.** `git ls-tree -r --name-only HEAD | grep -iE "app-showcase|appshowcase"` returns empty — the asset is not in the repo. **Substitution**: for in-app layout surfaces the source CSS Modules don't fully describe, I'll use the **live web client at https://fluxer.app** + the source component CSS Modules, noting each substitution inline.

## UNVERIFIED — needs source

These are NOT shipped as guessed values. They will be resolved by reading the
specific component CSS Modules in Phase 2/3/4 before any component that uses
them ships. Listed here so they're tracked.

### Typography

- **Named type scale (h1–h6 / per-role sizes).** Searched `globals.css` + `GenerateThemeVariables.ts`: only `--font-size-xs: 0.75rem`, base `16px`/`line-height: 1.5`, `.text-smol: 0.875rem`, `--textarea-line-height: 1.375rem`, `code: 0.85em`. There is no `--font-size-md`/`--font-size-lg` token. The real client sizes headings/labels ad-hoc per component (e.g. channel names, timestamps, headers) via `rem`/`em` in CSS Modules. **Resolution**: extract the exact `font-size`/`font-weight`/`line-height`/`letter-spacing` per component from its `.module.css` when building that component in Phase 2/3/4. Not invented here.

### Layout / breakpoints

- **Intermediate breakpoints (tablet etc.).** Searched `globals.css`: only `@media (max-width: 840px)` exists (the mobile/touch cutoff, `globals.css:490`). No formal breakpoint token scale. **Resolution**: per-component responsive rules live in their CSS Modules; extract when a responsive surface is built. The 840px cutoff is the only global one.

### Iconography

- **Canonical per-surface icon sizes.** Confirmed the icon SET is Phosphor (`@phosphor-icons/react`, `package.json:192`). Searched `globals.css` for an icon-size token: none. Icon sizes are set per-use in component CSS Modules. **Resolution**: extract the exact `width`/`height` for each icon surface (guild rail, channel row, header toolbar, user-area buttons) from the component's `.module.css` when building that surface. Not invented here.

### Code/ANSI palette

- **Full 16-color ANSI palette (`--ansi-fg-*`, `--ansi-bg-*`, bright variants).** Source is `GenerateColorSystem.ts:47-295` (DARK_CODE_TOKENS + LIGHT_CODE_TOKENS, ~80 entries). These are verbatim in the source, so they ARE sourced — but I ported only the core code tokens (`--code-text`, `--text-code`, `--code-inline-bg`, `--bg-code`, `--code-block-bg`, `--code-block-border`, `--code-block-highlight`, `--ansi-inverse-*`) into `tokens.css` for now, since the ANSI 16-color set is only consumed by code/terminal rendering. **Resolution**: port the full ANSI set into `tokens.css` in Phase 4 (code-block rendering) where it's consumed — values are already cited in `docs/design-system-extracted.md` and ready to copy 1:1.

### Coal + dark_legacy themes

- **`.theme-coal` and `.theme-dark_legacy` overrides.** Source is `GenerateColorSystem.ts:683-781`. These are alternative dark themes (coal = near-black; dark_legacy = the old Discord-style hue-220 dark). I ported the default dark (`:root`) + light (`.theme-light`) into `tokens.css` — the two themes the user toggles between by default. **Resolution**: port `.theme-coal` + `.theme-dark_legacy` if/when the app exposes them as user-selectable themes (the Settings appearance pane will tell us which themes are offered). Values are already cited and ready to copy 1:1.

### Generated CSS not committed

- **`color-system.css` / `message-layout.css` build artifacts.** These are generated by `scripts/GenerateColorSystem.ts` / `GenerateMessageLayoutCss.ts` and are NOT committed (verified: the `generated/` dir does not exist in the checkout). The generator scripts ARE the source of truth (read + cited). For `message-layout.css`, the generator delegates to `src/features/theme/layout/MessageLayoutCss.ts` which is checked out — I'll read it in Phase 4 (message list) for the message-layout-specific tokens. **Resolution**: cite the generator + the `MessageLayoutCss.ts` input for any message-layout token.

### Avatar status-dot geometry (per-size cx/cy/radius/borderWidth)

- **RESOLVED.** The geometry is in `scripts/GenerateAvatarMasks.ts:21-33` (`STATUS_CONFIG` table) + `calculateStatusGeometry` (`:128-162`). The earlier confusion was because `AvatarStatusLayout.ts` imports from `AvatarStatusGeometry.ts` (which is missing from the tree), but the *values* that file would return are fully present in the mask generator. The geometry derivation: `innerRadius = statusSize/2`, `outerRadius = cutoutRadius`, `borderWidth = cutoutRadius - innerRadius`, `cx = cy = cutoutCenter`.
- **Per-size table** (computed from `STATUS_CONFIG`, `GenerateAvatarMasks.ts:21-33`):

  | size | statusSize | cutoutRadius | cutoutCenter | innerRadius | outerRadius | borderWidth |
  |---|---|---|---|---|---|---|
  | 16 | 10 | 5 | 13 | 5 | 5 | 0 |
  | 20 | 10 | 5 | 17 | 5 | 5 | 0 |
  | 24 | 10 | 7 | 20 | 5 | 7 | 2 |
  | 32 | 10 | 8 | 27 | 5 | 8 | 3 |
  | 36 | 10 | 8 | 30 | 5 | 8 | 3 |
  | 40 | 12 | 9 | 34 | 6 | 9 | 3 |
  | 44 | 14 | 10 | 38 | 7 | 10 | 3 |
  | 48 | 14 | 10 | 42 | 7 | 10 | 3 |
  | 56 | 16 | 11 | 49 | 8 | 11 | 3 |
  | 80 | 16 | 19.2 | 68 | 8 | 19.2 | 11.2 |
  | 120 | 24 | 28.8 | 100 | 12 | 28.8 | 16.8 |

  (`LARGE_STATUS_CUTOUT_GUTTER_RATIO = 0.2`, `GenerateAvatarMasks.ts:19`; 80/120 cutoutRadius = `statusSize/2 + statusSize*0.2`.)

- These values are now used in the ported Avatar (`web/src/components/Avatar.tsx`) and cited there.

### Avatar masks (`GenerateAvatarMasks.ts`)

- **`scripts/GenerateAvatarMasks.ts` generates the static SVG masks** (`<mask id="svg-mask-avatar-status-round-32">` etc.) referenced by `BaseAvatar.tsx:532-542`. The generator IS checked out (`fluxer_app/scripts/GenerateAvatarMasks.ts` exists) but I have not yet read it. **Resolution**: read it before porting the Avatar to get the exact mask shapes (the cutout circle/rect geometry per size). Will cite it then.

## Items I will NOT invent

Per the anti-pattern rules, none of the above are shipped as guessed values.
Until each is sourced from its component CSS Module, the corresponding
component CSS uses only the already-sourced tokens above, never a made-up
number.